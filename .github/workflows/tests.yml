name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.3.0'
      SERVERPOD_PASSWORD_database: 'UrD2b4ELB8kOkxlckozDmsbguMBFv0qC'
      SERVERPOD_PASSWORD_redis: 'P3AKqgowihbHFZycArW79fZVuX1VMu1h'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: room_rental_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: room_rental_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: room_rental_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: room_rental_server
        run: serverpod generate

      - name: Run tests
        working-directory: room_rental_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: room_rental_server
        run: docker compose down -v
