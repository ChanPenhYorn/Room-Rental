class: ChatMessage
table: chat_message
fields:
  senderId: int
  sender: User?, relation(name=sender_messages, field=senderId)
  receiverId: int
  receiver: User?, relation(name=receiver_messages, field=receiverId)
  message: String
  sentAt: DateTime
  isRead: bool
